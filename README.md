# Math Adventures — AI-Powered Adaptive Learning Prototype (Fixed)

This is the same adaptive learning app, but updated for Streamlit v1.38+ compatibility.
All occurrences of `st.experimental_rerun()` have been replaced with `st.rerun()`.

See README in original version for full instructions.
